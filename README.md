# Bare-bones jsonrpc client/server pair

To test against StreamJsonRpc implementation from .NET

Based on implementations from ""

## To use, first install requirements:

```sh
pip install -r requirements.txt
```
